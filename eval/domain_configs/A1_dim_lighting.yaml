# A1_dim_lighting.yaml
name: dim_lighting
levels:
  weak:
    lighting:
      diffuse_scale: 0.45
      specular_scale: 0.45
      ambient_scale: 0.50
  medium:
    lighting:
      diffuse_scale: 0.20
      specular_scale: 0.15
      ambient_scale: 0.25
  strong:
    lighting:
      diffuse_scale: 0.08
      specular_scale: 0.05
      ambient_scale: 0.12