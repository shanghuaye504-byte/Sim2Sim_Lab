# 本地构建与交互测试
services:
  eval:
    image: yycatmax/sim2sim-eval:v1.1
    build:
      context: ..                            # 项目根目录
      dockerfile: docker/Dockerfile
    init: true
    tty: true
    network_mode: host
    volumes:
      - $PWD:/app
      - ${OPENPI_DATA_HOME:-~/.cache/openpi}:/app/.cache/openpi
    environment:
      # --- 新增这一行，告诉脚本在本地直接使用 /app，跳过 RunPod 的软链接逻辑 ---
      - APP_OVERRIDE=/app
      # ----------------------------------------------------------------------
      - OPENPI_DATA_HOME=/app/.cache/openpi
      - IS_DOCKER=true
      - LIBERO_CONFIG_PATH=/opt/libero_config
      - MUJOCO_GL=${MUJOCO_GL:-egl}
      - MUJOCO_EGL_DEVICE_ID=0
      - PYOPENGL_PLATFORM=egl
      - SERVER_PORT=8000
      - TASK_SUITE=${TASK_SUITE:-libero_spatial}
      - NUM_TRIALS=${NUM_TRIALS:-50}
      - POLICY_CONFIG=${POLICY_CONFIG:-pi05_libero}
      - POLICY_DIR=${POLICY_DIR:-gs://openpi-assets/checkpoints/pi05_libero}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]